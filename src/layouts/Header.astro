---
import SearchSwitch from '~/components/base/SearchSwitch.astro'
import { HEADER_LINKS, SITE, SOCIAL_LINKS } from '~/config'

const isLinkActive = (linkUrl: string, pathname: string) => {
  if (linkUrl === '/') return pathname === '/'
  return pathname === linkUrl || pathname.startsWith(`${linkUrl}/`)
}

const githubLink = SOCIAL_LINKS.find((item) => item.name === 'github')
---

<header class="antfu-header">
  <div class="antfu-header-inner">
    <a href="/" class="antfu-header-brand" aria-label={`${SITE.title} 首页`} transition:persist>
      <span class="antfu-header-logo" aria-hidden="true">
        <img src="/logo.svg" alt="" loading="eager" decoding="async" />
      </span>
      <span class="antfu-header-name">{SITE.title}</span>
    </a>

    <nav class="antfu-header-nav" aria-label="主导航">
      {
        HEADER_LINKS.map((link) => (
          <a
            href={link.url}
            class:list={[
              'antfu-header-link',
              isLinkActive(link.url, Astro.url.pathname) ? 'is-active' : '',
            ]}
            aria-current={isLinkActive(link.url, Astro.url.pathname) ? 'page' : undefined}
          >
            {link.name}
          </a>
        ))
      }
    </nav>

    <div class="antfu-header-actions" transition:persist="header-search-switch">
      <SearchSwitch labelClass="antfu-header-search-label" />

      {githubLink && (
        <a
          href={githubLink.url}
          class="antfu-header-icon antfu-header-github"
          target="_blank"
          rel="noreferrer"
          aria-label="GitHub"
          title="GitHub"
        >
          <span class="icon-[ri--github-fill] antfu-header-icon-glyph" aria-hidden="true"></span>
        </a>
      )}

      <button
        id="theme-toggle"
        class="antfu-header-icon antfu-header-theme-toggle"
        type="button"
        aria-label="切换主题"
        title="切换主题"
      >
        <span class="icon-[ri--sun-line] antfu-header-icon-glyph antfu-theme-icon-dark" aria-hidden="true"></span>
        <span class="icon-[ri--moon-line] antfu-header-icon-glyph antfu-theme-icon-light" aria-hidden="true"></span>
      </button>

      <button
        id="header-menu-button"
        class="antfu-header-menu-button"
        type="button"
        aria-expanded="false"
        aria-controls="header-mobile-menu"
        aria-label="切换导航菜单"
      >
        <span data-header-line="top"></span>
        <span data-header-line="bottom"></span>
      </button>
    </div>
  </div>

  <div id="header-mobile-menu" class="antfu-header-mobile-menu hidden">
    <nav aria-label="移动端导航">
      {
        HEADER_LINKS.map((link) => (
          <a
            href={link.url}
            class:list={[
              'antfu-header-mobile-link',
              isLinkActive(link.url, Astro.url.pathname) ? 'is-active' : '',
            ]}
            aria-current={isLinkActive(link.url, Astro.url.pathname) ? 'page' : undefined}
          >
            {link.name}
          </a>
        ))
      }
    </nav>
  </div>
</header>

<style>
  .antfu-header {
    position: relative;
    z-index: 20;
    padding: 0 1rem;
    color: var(--article-ink);
  }

  .antfu-header-inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    gap: clamp(1rem, 2.4vw, 2.5rem);
    width: min(100%, 72rem);
    min-height: 4.75rem;
    margin-inline: auto;
    padding: 0.85rem clamp(1rem, 2.5vw, 2rem);
    color: var(--article-ink);
    font-family: var(--font-geist-mono);
    font-size: 0.75rem;
    letter-spacing: 0.04em;
  }

  .antfu-header-brand {
    display: inline-flex;
    min-width: 0;
    align-items: center;
    justify-self: start;
    gap: 0.85rem;
    color: var(--article-ink);
    text-decoration: none;
    transition:
      color 200ms ease,
      opacity 200ms ease;
  }

  .antfu-header-brand:hover {
    color: var(--article-ink);
    opacity: 0.78;
  }

  .antfu-header-logo {
    display: inline-grid;
    width: 2.25rem;
    height: 2.25rem;
    flex: 0 0 auto;
    place-items: center;
  }

  .antfu-header-logo img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  :global(html.dark) .antfu-header-logo img {
    filter: invert(0.88) sepia(0.08) saturate(0.55);
  }

  .antfu-header-name {
    overflow: hidden;
    color: currentColor;
    font-family: var(--font-geist-mono);
    font-size: clamp(1.05rem, 1.7vw, 1.3rem);
    font-weight: 600;
    letter-spacing: 0;
    line-height: 1;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .antfu-header-nav {
    display: none;
    align-items: center;
    justify-content: center;
    gap: 0.35rem;
  }

  @media (min-width: 860px) {
    .antfu-header-nav { display: flex; }
  }

  .antfu-header-link {
    display: inline-flex;
    min-height: 2.35rem;
    align-items: center;
    justify-content: center;
    border: 1px solid transparent;
    border-radius: 999px;
    padding-inline: clamp(0.75rem, 1.2vw, 1.05rem);
    color: var(--article-soft);
    text-decoration: none;
    white-space: nowrap;
    opacity: 0.88;
    transition:
      background-color 200ms ease,
      border-color 200ms ease,
      color 200ms ease,
      opacity 200ms ease;
  }

  .antfu-header-link:hover {
    border-color: var(--article-rule);
    background: light-dark(rgb(31 31 29 / 0.035), rgb(232 228 220 / 0.045));
    color: var(--article-ink);
    opacity: 1;
  }

  .antfu-header-link.is-active {
    border-color: light-dark(rgb(31 31 29 / 0.14), rgb(232 228 220 / 0.14));
    background: light-dark(rgb(31 31 29 / 0.055), rgb(232 228 220 / 0.06));
    color: var(--article-ink);
    opacity: 1;
  }

  .antfu-header-brand:focus-visible,
  .antfu-header-link:focus-visible,
  .antfu-header-icon:focus-visible,
  :global(.antfu-header-search:focus-visible),
  .antfu-header-menu-button:focus-visible,
  .antfu-header-mobile-link:focus-visible {
    outline: 1px solid var(--article-accent);
    outline-offset: 0.35rem;
  }

  .antfu-header-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.35rem;
    height: 2.35rem;
    padding: 0;
    color: var(--article-soft);
    background: transparent;
    border: 1px solid var(--article-rule);
    border-radius: 999px;
    cursor: pointer;
    text-decoration: none;
    transition:
      background-color 200ms ease,
      border-color 200ms ease,
      color 200ms ease,
      opacity 200ms ease;
  }

  .antfu-header-icon:hover {
    border-color: light-dark(rgb(31 31 29 / 0.22), rgb(232 228 220 / 0.24));
    background: light-dark(rgb(31 31 29 / 0.035), rgb(232 228 220 / 0.045));
    color: var(--article-ink);
  }

  .antfu-header-icon-glyph {
    display: inline-block;
    width: 1em;
    height: 1em;
    font-size: 0.95rem;
    line-height: 1;
  }

  /* 主题图标根据 html 上的 .dark / .light 类切换显示 */
  .antfu-theme-icon-light {
    display: none;
  }

  :global(html.dark) .antfu-theme-icon-dark {
    display: inline-block;
  }

  :global(html.dark) .antfu-theme-icon-light {
    display: none;
  }

  :global(html.light) .antfu-theme-icon-dark {
    display: none;
  }

  :global(html.light) .antfu-theme-icon-light {
    display: inline-block;
  }

  .antfu-header-actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    justify-self: end;
    gap: 0.55rem;
  }

  .antfu-header-menu-button {
    display: inline-flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 4px;
    width: 2.35rem;
    height: 2.35rem;
    padding: 0;
    color: var(--article-soft);
    background: transparent;
    border: 1px solid var(--article-rule);
    border-radius: 999px;
    cursor: pointer;
    transition:
      background-color 200ms ease,
      border-color 200ms ease,
      color 200ms ease;
  }

  .antfu-header-menu-button:hover {
    border-color: light-dark(rgb(31 31 29 / 0.22), rgb(232 228 220 / 0.24));
    background: light-dark(rgb(31 31 29 / 0.035), rgb(232 228 220 / 0.045));
    color: var(--article-ink);
  }

  @media (min-width: 860px) {
    .antfu-header-menu-button { display: none; }
  }

  .antfu-header-menu-button span {
    display: block;
    width: 0.95rem;
    height: 1px;
    background: currentColor;
    transition: transform 200ms ease;
  }

  .antfu-header-mobile-menu {
    width: min(100%, 72rem);
    margin-inline: auto;
    padding: 0.6rem clamp(1rem, 2.5vw, 2rem) 0;
  }

  @media (min-width: 860px) {
    .antfu-header-mobile-menu { display: none !important; }
  }

  .antfu-header-mobile-menu nav {
    overflow: hidden;
    border: 1px solid light-dark(rgb(31 31 29 / 0.08), rgb(232 228 220 / 0.1));
    border-radius: 0.35rem;
    background: light-dark(rgb(255 255 255 / 0.92), rgb(16 16 15 / 0.94));
    box-shadow: 0 12px 28px light-dark(rgb(31 31 29 / 0.06), rgb(0 0 0 / 0.18));
  }

  .antfu-header-mobile-link {
    display: block;
    padding: 0.8rem 1rem;
    color: var(--article-soft);
    font-family: var(--font-geist-mono);
    font-size: 0.8125rem;
    letter-spacing: 0.04em;
    text-decoration: none;
    border-block-end: 1px solid var(--article-rule);
    opacity: 0.72;
    transition:
      color 200ms ease,
      opacity 200ms ease;
  }

  .antfu-header-mobile-link:hover,
  .antfu-header-mobile-link.is-active {
    color: var(--article-ink);
    opacity: 1;
  }

  :global(.antfu-header-search) {
    display: inline-flex;
    min-height: 2.35rem;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    border: 1px solid var(--article-rule);
    border-radius: 999px;
    padding-inline: 1rem;
    font-family: var(--font-geist-mono);
    font-size: 0.75rem;
    letter-spacing: 0.04em;
    color: var(--article-soft);
    background: transparent;
    cursor: pointer;
    transition:
      background-color 200ms ease,
      border-color 200ms ease,
      color 200ms ease,
      opacity 200ms ease;
  }

  :global(.antfu-header-search:hover) {
    border-color: light-dark(rgb(31 31 29 / 0.22), rgb(232 228 220 / 0.24));
    background: light-dark(rgb(31 31 29 / 0.035), rgb(232 228 220 / 0.045));
    color: var(--article-ink);
  }

  :global(.antfu-header-search-label) {
    display: inline;
  }

  :global(.antfu-header-search-icon) {
    width: 0.95rem;
    height: 0.95rem;
    flex: 0 0 auto;
  }

  @media (max-width: 859px) {
    .antfu-header {
      padding-inline: 0.75rem;
    }

    .antfu-header-inner {
      grid-template-columns: minmax(0, 1fr) auto;
      min-height: 4.25rem;
      padding: 0.7rem 0.85rem;
    }

    .antfu-header-actions {
      gap: 0.35rem;
    }

    .antfu-header-logo {
      width: 2rem;
      height: 2rem;
    }

    .antfu-header-name {
      font-size: 1rem;
    }

    :global(.antfu-header-search) {
      width: 2.35rem;
      min-width: 2.35rem;
      padding-inline: 0;
    }

    :global(.antfu-header-search-label),
    :global(.antfu-header-search .antfu-header-search-label) {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
  }

  @media (max-width: 420px) {
    .antfu-header-brand {
      gap: 0.55rem;
    }

    .antfu-header-logo {
      width: 1.75rem;
      height: 1.75rem;
    }

    .antfu-header-name {
      max-width: 8.5rem;
    }

    .antfu-header-icon,
    .antfu-header-menu-button,
    :global(.antfu-header-search) {
      width: 2.1rem;
      min-width: 2.1rem;
      height: 2.1rem;
      min-height: 2.1rem;
    }
  }
</style>

<script>
  const initHeaderMenu = () => {
    const menuButton = document.getElementById('header-menu-button')
    const mobileMenu = document.getElementById('header-mobile-menu')
    const topLine = document.querySelector('[data-header-line="top"]')
    const bottomLine = document.querySelector('[data-header-line="bottom"]')

    if (!menuButton || !mobileMenu) return

    const syncMenuState = (expanded: boolean) => {
      menuButton.setAttribute('aria-expanded', expanded ? 'true' : 'false')
      mobileMenu.classList.toggle('hidden', !expanded)
      topLine?.classList.toggle('-translate-y-[3px]', !expanded)
      topLine?.classList.toggle('translate-y-0', expanded)
      topLine?.classList.toggle('rotate-45', expanded)
      bottomLine?.classList.toggle('translate-y-[3px]', !expanded)
      bottomLine?.classList.toggle('translate-y-0', expanded)
      bottomLine?.classList.toggle('-rotate-45', expanded)
    }

    syncMenuState(false)

    menuButton.onclick = () => {
      const expanded = menuButton.getAttribute('aria-expanded') === 'true'
      syncMenuState(!expanded)
    }

    mobileMenu.querySelectorAll<HTMLAnchorElement>('a').forEach((link) => {
      link.onclick = () => syncMenuState(false)
    })
  }

  initHeaderMenu()
  document.addEventListener('astro:page-load', initHeaderMenu)
</script>
